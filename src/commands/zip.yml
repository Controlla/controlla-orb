description: >
  Compress files

steps:
  - run: zip aws.zip -r * .[^.]* --exclude=vendor/* --exclude=node_modules/* --exclude=frontend/* --exclude=.circleci/* --exclude=.elasticbeanstalk/* --exclude=.git/*
