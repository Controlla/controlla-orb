description: >
  This command make release of proyect.

steps:
  - restore_cache:
      name: Restore npm dependencies
      key: npm-{{ checksum "frontend/package-lock.json" }}-{{ .Environment.CACHE_VERSION_NPM }}
      paths:
        - frontend/node_modules
