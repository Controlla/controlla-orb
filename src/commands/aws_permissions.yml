description: >
  Add permmissions to aws scripts

steps:
  - run: chmod +x .platform/hooks/prebuild/*.sh
  - run: chmod +x .platform/hooks/predeploy/*.sh
  - run: chmod +x .platform/hooks/postdeploy/*.sh
