description: >
  Install npm depencies

parameters:
  directory:
    description: Select a specific directory.
    default: "frontend"
    type: string

steps:
  - run: "cd <<parameters.directory>> && npm install"
